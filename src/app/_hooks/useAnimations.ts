/**
 * Central export for all animation hooks
 * Import from this file for cleaner imports in components
 */

export { useParallax, useHeroParallax, useImageParallax } from "./useParallax";
export { useFadeIn, useFadeInUp, useSimpleFade } from "./useFadeIn";
export {
	useStaggerReveal,
	useCardReveal,
	useGalleryReveal,
} from "./useStaggerReveal";
export {
	useTextReveal,
	useHeroTextReveal,
	useSectionHeaderReveal,
} from "./useTextReveal";
export { useScrollIndicator } from "./useScrollIndicator";
